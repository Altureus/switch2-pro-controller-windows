# The WIRED USB bridge (bridge.py) needs ZERO Python packages
# -- it is pure ctypes. You only need anything here for the optional WIRELESS
# (Bluetooth LE) path.
#
#   pip install -r requirements.txt
#
# Wireless (Bluetooth LE) path only -- ble_bridge.py (via launch.py / Start (Auto-detect).bat):
bleak>=0.22        # Windows BLE; pulls in winrt-runtime automatically
